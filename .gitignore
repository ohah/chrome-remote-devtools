# 참조 코드 (원본 저장소에서 가져온 코드)
reference/

# Rust
/target/
**/*.rs.bk

# Bun/Node
node_modules/
dist/
build/
.bun/
*.bun

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment
.env
.env.local

# mise
.mise/

# DevTools 빌드 산출물 (소스 빌드 결과물은 제외, bundled는 Git에 포함)
devtools/devtools-frontend/out/
devtools/.cipd
devtools/.cipd/tagcache.db
devtools/.gclient_entries
devtools/.gclient_previous_sync_commits
devtools/.gcs_entries

packages/inspector/dist/
# bundled는 Git에 포함되므로 제외하지 않음 / bundled is included in Git, so don't ignore it
# devtools/bundled/

# Documentation build output
node_modules
dist/
doc_build/

# Playwright test results / Playwright 테스트 결과
test-results/
playwright-report/
.playwright/

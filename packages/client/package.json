{
  "name": "@ohah/chrome-remote-devtools-client",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target browser --format iife --minify",
    "build:watch": "bun build src/index.ts --outdir dist --target browser --format iife --minify --watch",
    "clean": "rm -rf dist",
    "test": "bun test"
  },
  "dependencies": {
    "@ohah/chrome-remote-devtools-client-rrweb": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
